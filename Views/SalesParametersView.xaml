<UserControl x:Class="MultiFuelMaster.Views.SalesParametersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1100"
             Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Параметры продаж" Style="{StaticResource MaterialDesignHeadline4TextBlock}"/>
                <TextBlock Text="Цены, округление, чек, ограничения (UI-скелет)" Opacity="0.72" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Сохранить"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Padding="14,6"
                        Margin="0,0,10,0"/>
                <Button Content="Закрыть"
                        Command="{Binding CloseCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Padding="14,6"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Padding="12" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Цены по топливу" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,12"/>
                    <DataGrid ItemsSource="{Binding Prices}" SelectedItem="{Binding Selected}" AutoGenerateColumns="False" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Топливо" Binding="{Binding FuelName}" Width="*"/>
                            <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="160"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <TextBlock TextWrapping="Wrap" Opacity="0.68" Margin="0,12,0,0">
                        Дополнения из GasKit: профили цен, расписания (дневные/ночные), права на изменение цен, журнал изменений.
                    </TextBlock>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" Padding="16">
                <StackPanel>
                    <TextBlock Text="Правила продажи" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,12"/>

                    <CheckBox Content="Включить округление" IsChecked="{Binding EnableRounding}" Margin="0,0,0,12"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                        <TextBlock Text="Шаг округления:" Opacity="0.75" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Text="{Binding RoundingStep}" Width="90" Height="30"/>
                    </StackPanel>

                    <CheckBox Content="Печать чека" IsChecked="{Binding EnableReceiptPrinting}" Margin="0,0,0,12"/>

                    <Separator Margin="0,6,0,12"/>

                    <TextBlock Text="Будущие параметры" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <TextBlock Text="• Лимиты: максимальный объем/сумма на транзакцию" Opacity="0.78"/>
                    <TextBlock Text="• Автозапрет продажи при тревоге резервуара" Opacity="0.78"/>
                    <TextBlock Text="• Режимы: Volume / Money / Full tank" Opacity="0.78"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
