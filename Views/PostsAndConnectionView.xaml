<UserControl x:Class="MultiFuelMaster.Views.PostsAndConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1100"
             Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Посты и связь" Style="{StaticResource MaterialDesignHeadline4TextBlock}"/>
                <TextBlock Text="COM-порты, протоколы, адреса и тайминги (UI-скелет)" Opacity="0.72" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Добавить"
                        Command="{Binding AddPostCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Padding="14,6"
                        Margin="0,0,10,0"/>
                <Button Content="Удалить"
                        Command="{Binding RemoveSelectedCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Padding="14,6"
                        Margin="0,0,10,0"/>
                <Button Content="Сохранить"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Padding="14,6"
                        Margin="0,0,10,0"/>
                <Button Content="Закрыть"
                        Command="{Binding CloseCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Padding="14,6"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Padding="12" Margin="0,0,12,0">
                <DataGrid ItemsSource="{Binding Posts}" SelectedItem="{Binding Selected}" AutoGenerateColumns="False" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Пост" Binding="{Binding PostNumber}" Width="70"/>
                        <DataGridTextColumn Header="COM" Binding="{Binding ComPort}" Width="100"/>
                        <DataGridTextColumn Header="Протокол" Binding="{Binding Protocol}" Width="*"/>
                        <DataGridTextColumn Header="CH" Binding="{Binding Channel}" Width="70"/>
                        <DataGridTextColumn Header="ID" Binding="{Binding DeviceId}" Width="70"/>
                        <DataGridTextColumn Header="Период, мс" Binding="{Binding PollingIntervalMs}" Width="120"/>
                        <DataGridTextColumn Header="Таймаут, мс" Binding="{Binding ResponseTimeoutMs}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" Padding="16">
                <StackPanel>
                    <TextBlock Text="Параметры выбранного поста" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,12"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Пост:" Opacity="0.75" Margin="0,0,0,10"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Selected.PostNumber}" Height="30" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="COM:" Opacity="0.75" Margin="0,0,0,10"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Selected.ComPort}" Height="30" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Протокол:" Opacity="0.75" Margin="0,0,0,10"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Selected.Protocol}" Height="30" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="CHANNEL:" Opacity="0.75" Margin="0,0,0,10"/>
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Selected.Channel}" Height="30" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="DEV_ID:" Opacity="0.75" Margin="0,0,0,10"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Selected.DeviceId}" Height="30" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Тайминги:" Opacity="0.75"/>
                        <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal">
                            <TextBox Text="{Binding Selected.PollingIntervalMs}" Width="120" Height="30" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding Selected.ResponseTimeoutMs}" Width="120" Height="30"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock TextWrapping="Wrap" Opacity="0.68" Margin="0,12,0,0">
                        Важно для 8 постов: все опросы и парсинг протокола должны быть неблокирующими, 
                        а обновление UI — через один поток сообщений (Dispatcher) с буферизацией.
                    </TextBlock>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
