<UserControl x:Class="MultiFuelMaster.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <ScrollViewer>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" 
                     Text="Настройки приложения" 
                     Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                     Margin="0,0,0,24"/>
            
            <StackPanel Grid.Row="1">
                <!-- Application Info -->
                <materialDesign:Card Margin="0,0,0,16">
                    <StackPanel Margin="16">
                        <TextBlock Text="Информация о приложении" 
                                 Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                 Margin="0,0,0,16"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Версия:" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ApplicationVersion}" 
                                     VerticalAlignment="Center" Margin="16,0,0,0"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="База данных:" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DatabasePath}" 
                                     VerticalAlignment="Center" Margin="16,0,0,0"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Хранение данных:" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" 
                                     Text="{Binding DataRetentionDays, StringFormat='{}{0} дней'}" 
                                     VerticalAlignment="Center" Margin="16,0,0,0"/>
                        </Grid>
                    </StackPanel>
                </materialDesign:Card>
                
                <!-- Display Settings -->
                <materialDesign:Card Margin="0,0,0,16">
                    <StackPanel Margin="16">
                        <TextBlock Text="Настройки отображения" 
                                 Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                 Margin="0,0,0,16"/>
                        
                        <CheckBox IsChecked="{Binding AutoRefreshEnabled}" 
                                Content="Автоматическое обновление данных"
                                Margin="0,0,0,8"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="24,0,0,0">
                            <TextBlock Text="Интервал обновления:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Text="{Binding RefreshIntervalMinutes}" 
                                   Width="60" 
                                   materialDesign:HintAssist.Hint="минут"/>
                            <TextBlock Text="минут" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>
                
                <!-- Database Info -->
                <materialDesign:Card Margin="0,0,0,16">
                    <StackPanel Margin="16">
                        <TextBlock Text="Информация о базе данных" 
                                 Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                 Margin="0,0,0,16"/>
                        
                        <TextBlock TextWrapping="Wrap" 
                                 Opacity="0.68">
                            Приложение использует SQLite базу данных для хранения всей информации о станциях, топливе и транзакциях. 
                            Все данные доступны только для чтения через пользовательский интерфейс. 
                            Изменение данных возможно только через прямое подключение к базе данных.
                        </TextBlock>
                    </StackPanel>
                </materialDesign:Card>
                
                <!-- Help -->
                <materialDesign:Card>
                    <StackPanel Margin="16">
                        <TextBlock Text="Помощь" 
                                 Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                 Margin="0,0,0,16"/>
                        
                        <TextBlock TextWrapping="Wrap" 
                                 Opacity="0.68">
                            Это приложение предназначено для мониторинга и анализа работы топливных станций. 
                            Используйте навигационное меню сверху для перехода между разделами.
                        </TextBlock>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>