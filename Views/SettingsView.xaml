<UserControl x:Class="MultiFuelMaster.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1100"
             Background="{DynamicResource MaterialDesignPaper}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="Сервис" Style="{StaticResource MaterialDesignHeadline4TextBlock}"/>
                <TextBlock Text="Сведения о системе и базовые параметры приложения" Opacity="0.72" Margin="0,4,0,0"/>
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- System info -->
                <materialDesign:Card Grid.Column="0" Padding="16" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="Системная информация" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,12"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Версия приложения" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ApplicationVersion}" Opacity="0.8" Margin="0,0,0,10"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Runtime" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding FrameworkVersion}" Opacity="0.8" Margin="0,0,0,10" TextWrapping="Wrap"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="База данных" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DatabasePath}" Opacity="0.8" Margin="0,0,0,10" TextWrapping="Wrap"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Логи" FontWeight="SemiBold"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding LogDirectory}" Opacity="0.8" TextWrapping="Wrap"/>
                        </Grid>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Preferences -->
                <materialDesign:Card Grid.Column="1" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Параметры" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,12"/>

                        <CheckBox Content="Автообновление данных" IsChecked="{Binding AutoRefreshEnabled}" Margin="0,0,0,12"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="Интервал обновления (мин):" Opacity="0.75" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding RefreshIntervalMinutes}" Width="90" Height="30"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="Хранить данные (дней):" Opacity="0.75" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding DataRetentionDays}" Width="90" Height="30"/>
                        </StackPanel>

                        <TextBlock TextWrapping="Wrap" Opacity="0.68" Margin="0,12,0,0">
                            Здесь будет добавлено: резервное копирование/восстановление базы, экспорт настроек, выбор темы,
                            и сервисные действия по диагностике связи и логированию.
                        </TextBlock>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
