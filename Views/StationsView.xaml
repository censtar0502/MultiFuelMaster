<UserControl x:Class="MultiFuelMaster.Views.StationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Stations List -->
        <materialDesign:Card Grid.Column="0" Margin="0,0,10,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" 
                         Text="Станции" 
                         Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                         Margin="16,16,16,8"/>
                
                <ListBox Grid.Row="1"
                        ItemsSource="{Binding Stations}"
                        SelectedItem="{Binding SelectedStation}"
                        SelectionMode="Single"
                        Margin="16,0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,8">
                                <TextBlock Text="{Binding Name}" 
                                         Style="{StaticResource MaterialDesignBody2TextBlock}"
                                         FontWeight="Bold"/>
                                <TextBlock Text="{Binding Address}" 
                                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                         Opacity="0.68"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                
                <Button Grid.Row="2"
                       Style="{StaticResource MaterialDesignFlatButton}"
                       Command="{Binding RefreshDataCommand}"
                       Margin="16,8">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh" Margin="0,0,8,0"/>
                        <TextBlock Text="Обновить"/>
                    </StackPanel>
                </Button>
            </Grid>
        </materialDesign:Card>
        
        <!-- Dispensers Details -->
        <materialDesign:Card Grid.Column="1">
            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" 
                         Text="{Binding SelectedStation.Name, TargetNullValue='Выберите станцию'}" 
                         Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                         Margin="0,0,0,16"/>
                
                <DataGrid Grid.Row="1"
                         ItemsSource="{Binding Dispensers}"
                         AutoGenerateColumns="False"
                         IsReadOnly="True"
                         GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="№" Binding="{Binding DispenserNumber}" Width="50"/>
                        <DataGridTextColumn Header="Тип топлива" Binding="{Binding FuelType.Name}" Width="*"/>
                        <DataGridTextColumn Header="Цена за литр" Binding="{Binding CurrentPrice, StringFormat='{}{0:N2}'}" Width="100"/>
                        <DataGridTextColumn Header="Остаток, л" Binding="{Binding CurrentVolume, StringFormat='{}{0:N2}'}" Width="100"/>
                        <DataGridTextColumn Header="Всего, л" Binding="{Binding TotalVolume, StringFormat='{}{0:N2}'}" Width="100"/>
                        <DataGridTextColumn Header="Работает" Binding="{Binding IsOperational}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>