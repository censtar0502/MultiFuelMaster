<UserControl x:Class="MultiFuelMaster.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Filter Panel -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" Margin="16">
                <DatePicker SelectedDate="{Binding StartDate}" 
                           materialDesign:HintAssist.Hint="Дата с"
                           Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                           Margin="0,0,16,0"/>
                
                <DatePicker SelectedDate="{Binding EndDate}" 
                           materialDesign:HintAssist.Hint="Дата по"
                           Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                           Margin="0,0,16,0"/>
                
                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding ApplyDateFilterCommand}"
                        Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Filter" Margin="0,0,8,0"/>
                        <TextBlock Text="Фильтровать"/>
                    </StackPanel>
                </Button>
                
                <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Command="{Binding RefreshTransactionsCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh" Margin="0,0,8,0"/>
                        <TextBlock Text="Обновить"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </materialDesign:Card>
        
        <!-- Transactions DataGrid -->
        <materialDesign:Card Grid.Row="1">
            <DataGrid ItemsSource="{Binding Transactions}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     GridLinesVisibility="Horizontal"
                     Margin="16">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Дата" Binding="{Binding TransactionDate, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" Width="140"/>
                    <DataGridTextColumn Header="Станция" Binding="{Binding Dispenser.Station.Name}" Width="*"/>
                    <DataGridTextColumn Header="Раздатчик" Binding="{Binding Dispenser.DispenserNumber}" Width="80"/>
                    <DataGridTextColumn Header="Топливо" Binding="{Binding Dispenser.FuelType.Name}" Width="100"/>
                    <DataGridTextColumn Header="Объем, л" Binding="{Binding Volume, StringFormat='{}{0:N2}'}" Width="80"/>
                    <DataGridTextColumn Header="Цена, руб." Binding="{Binding PricePerLiter, StringFormat='{}{0:N2}'}" Width="90"/>
                    <DataGridTextColumn Header="Сумма, руб." Binding="{Binding TotalAmount, StringFormat='{}{0:N2}'}" Width="100"/>
                    <DataGridTextColumn Header="Гос. номер" Binding="{Binding VehiclePlate}" Width="100"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>