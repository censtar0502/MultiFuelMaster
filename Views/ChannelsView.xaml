<UserControl x:Class="MultiFuelMaster.Views.ChannelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1100"
             Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Каналы и пистолеты" Style="{StaticResource MaterialDesignHeadline4TextBlock}"/>
                <TextBlock Text="Привязка каналов к топливу и резервуарам (UI-скелет)" Opacity="0.72" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Добавить"
                        Command="{Binding AddChannelCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Padding="14,6"
                        Margin="0,0,10,0"/>
                <Button Content="Удалить"
                        Command="{Binding RemoveSelectedCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Padding="14,6"
                        Margin="0,0,10,0"/>
                <Button Content="Сохранить"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Padding="14,6"
                        Margin="0,0,10,0"/>
                <Button Content="Закрыть"
                        Command="{Binding CloseCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Padding="14,6"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Padding="12" Margin="0,0,12,0">
                <DataGrid ItemsSource="{Binding Channels}" SelectedItem="{Binding Selected}" AutoGenerateColumns="False" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Канал" Binding="{Binding ChannelNumber}" Width="90"/>
                        <DataGridTextColumn Header="Пистолет" Binding="{Binding NozzleNumber}" Width="110"/>
                        <DataGridTextColumn Header="Топливо" Binding="{Binding FuelName}" Width="*"/>
                        <DataGridTextColumn Header="Резервуар" Binding="{Binding TankNumber}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" Padding="16">
                <StackPanel>
                    <TextBlock Text="Параметры" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,12"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Канал:" Opacity="0.75" Margin="0,0,0,10"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Selected.ChannelNumber}" Height="30" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Пистолет:" Opacity="0.75" Margin="0,0,0,10"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Selected.NozzleNumber}" Height="30" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Топливо:" Opacity="0.75" Margin="0,0,0,10"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Selected.FuelName}" Height="30" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Резервуар:" Opacity="0.75"/>
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Selected.TankNumber}" Height="30"/>
                    </Grid>

                    <TextBlock TextWrapping="Wrap" Opacity="0.68" Margin="0,12,0,0">
                        Дополнения из GasKit: блокировка отпуска при низком уровне резервуара, 
                        запрет смешения топлива, учёт калибровки датчиков.
                    </TextBlock>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
